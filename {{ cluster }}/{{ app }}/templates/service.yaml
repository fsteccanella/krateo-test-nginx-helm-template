apiVersion: v1
kind: Service
metadata:
  name: {{ include "krateo-test-nginx-helm-template.fullname" . }}
  labels:
    app: {{ include "krateo-test-nginx-helm-template.name" . }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 80
  selector:
    app: {{ include "krateo-test-nginx-helm-template.name" . }}
